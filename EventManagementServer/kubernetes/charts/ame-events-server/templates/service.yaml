apiVersion: v1
kind: Service
metadata:
  name: {{ include "ame-events-server.fullname" . }}
  labels:
    {{- include "ame-events-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ame-events-server.selectorLabels" . | nindent 4 }}
