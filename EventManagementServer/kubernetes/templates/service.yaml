apiVersion: v1
kind: Service
metadata:
  name: {{ include "ame-event-management-server.fullname" . }}
  labels: {{ include "ame-event-management-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{ include "ame-event-management-server.selectorLabels" . | nindent 4 }}
